"""Deterministic command implementations.

Each module is small, stdlib-only (or relies on lockedin's own packages),
and runs without an LLM. The skill calls them via Bash; the user can also
call them directly from any terminal.
"""
